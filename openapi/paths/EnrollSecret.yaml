post:
  tags:
    - Secrets
  summary: Enroll a secret for recovery.
  requestBody:
    description: ''
    content:
      application/json; x-api-version=2.2:
        schema:
          $ref: ..\components\schemas\EnrollmentRecord.yaml
      text/json; x-api-version=2.2:
        schema:
          $ref: ..\components\schemas\EnrollmentRecord.yaml
      application/*+json; x-api-version=2.2:
        schema:
          $ref: ..\components\schemas\EnrollmentRecord.yaml
  responses:
    '200':
      description: OK
